[project]
name = "red-alert-webhook"
version = "1.0.0"
description = "HTTP webhook notifications for Red Alert alert events"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
]
