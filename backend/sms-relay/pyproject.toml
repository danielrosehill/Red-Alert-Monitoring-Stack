[project]
name = "red-alert-sms-relay"
version = "1.0.0"
description = "SMS and voice call notifications via Twilio for Red Alert alert events"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "twilio>=9.0",
]
