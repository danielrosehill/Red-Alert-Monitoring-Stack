[project]
name = "snapcast-tts"
version = "1.0.0"
description = "Snapcast TTS announcements for Red Alert events"
requires-python = ">=3.12"
dependencies = [
    "httpx",
    "fastapi",
    "uvicorn",
    "edge-tts",
    "websockets",
]
