[project]
name = "red-alert-actuator"
version = "2.0.0"
description = "Home Assistant bridge for Pikud HaOref alerts — sets HA input_select state via REST API"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
]
