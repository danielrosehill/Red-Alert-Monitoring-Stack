[project]
name = "red-alert-actuator"
version = "1.0.0"
description = "Physical alert outputs (Snapcast TTS + MQTT lights) for Pikud HaOref alerts"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "paho-mqtt>=2.0",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
]
