# =============================================================================
# Red Alert Monitoring Stack — Docker Compose Override (Example)
# =============================================================================
# Copy this file to override.yml to customize the stack without modifying
# the base compose files. Use with: docker compose -f compose/default.yml -f compose/override.yml
#
#   cp compose/override.example.yml compose/override.yml
#
# Common customizations are shown below. Uncomment and modify as needed.
# =============================================================================

services:
  # -- InfluxDB: healthcheck + external volumes (reuse existing data) ----------
  # influxdb:
  #   volumes:
  #     - influxdb-data:/var/lib/influxdb2
  #     - influxdb-config:/etc/influxdb2
  #   environment:
  #     - DOCKER_INFLUXDB_INIT_RETENTION=0
  #   healthcheck:
  #     test: ["CMD", "influx", "ping"]
  #     interval: 15s
  #     timeout: 5s
  #     retries: 5
  #     start_period: 30s

  # -- Geodash: override InfluxDB connection or add extra env vars -------------
  # geodash:
  #   environment:
  #     - INFLUX_URL=http://influxdb:8086
  #     - INFLUX_TOKEN=${INFLUXDB_TOKEN}
  #     - INFLUX_ORG=${INFLUXDB_ORG}
  #     - INFLUX_BUCKET=${INFLUXDB_BUCKET}
  #     - POLL_INTERVAL=3
  #     - PYTHONUNBUFFERED=1
  #     - GEMINI_API_KEY=${GEMINI_API_KEY}
  #   depends_on:
  #     influxdb:
  #       condition: service_healthy
  #     oref-proxy:
  #       condition: service_started

  # -- Override actuator settings (e.g., custom HA entity) ---------------------
  # actuator:
  #   environment:
  #     - HASS_HOST=${HASS_HOST}
  #     - HASS_TOKEN=${HASS_TOKEN}
  #     - HASS_ENTITY=${HASS_ENTITY:-input_select.red_alert_state}

  # -- Add Cloudflare Tunnel for remote access ---------------------------------
  # cloudflared:
  #   image: cloudflare/cloudflared:latest
  #   container_name: cloudflared
  #   restart: unless-stopped
  #   command: tunnel run
  #   volumes:
  #     - ../cloudflared:/etc/cloudflared:ro
  #   networks:
  #     - redalert

  # -- Override RSS feeds (use numbered env vars) ------------------------------
  # rss-cache:
  #   environment:
  #     - RSS_FEED_1_URL=https://www.timesofisrael.com/feed/
  #     - RSS_FEED_1_NAME=Times of Israel
  #     - RSS_FEED_2_URL=https://www.jns.org/feed/
  #     - RSS_FEED_2_NAME=JNS
  #     - RSS_FEED_3_URL=https://www.ynetnews.com/rss/news
  #     - RSS_FEED_3_NAME=Ynet
  #     - RSS_POLL_INTERVAL=120

  # -- Add a Grafana dashboard for InfluxDB visualization ----------------------
  # grafana:
  #   image: grafana/grafana:latest
  #   container_name: grafana
  #   restart: unless-stopped
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - GF_SECURITY_ADMIN_PASSWORD=admin
  #   volumes:
  #     - grafana-data:/var/lib/grafana
  #   networks:
  #     - redalert

  # -- Resource limits ---------------------------------------------------------
  # actuator:
  #   deploy:
  #     resources:
  #       limits:
  #         memory: 256M
  #         cpus: '0.5'

# -- External volumes (uncomment if reusing existing InfluxDB data) ----------
# volumes:
#   influxdb-data:
#     external: true
#     name: geodash_influxdb-data
#   influxdb-config:
#     external: true
#     name: geodash_influxdb-config
#   grafana-data:
