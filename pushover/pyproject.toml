[project]
name = "red-alert-pushover"
version = "0.1.0"
description = "Standalone Pushover notifier for volumetric Oref alerts"
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
